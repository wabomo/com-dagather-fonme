<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<settings>
		<!-- 配置使全局的映射器启用或禁用缓存 默认：true -->
		<setting name="cacheEnabled" value="false" />
		<!-- 全局启用或禁用延迟加载。当禁用时，所有关联对象都会即时加载 默认：true -->
		<setting name="lazyLoadingEnabled" value="true" />
		<!-- 允许或不允许多种结果集从一个单独的语句中返回（需要适合的驱动） 默认：true -->
		<setting name="multipleResultSetsEnabled" value="true" />
		<!-- 使用列标签代替列名。不同的驱动在这方便表现不同。参考驱动文档或充分测试两种方法来决定所使用的驱动 默认：true -->
		<setting name="useColumnLabel" value="true" />
		<!-- 允许JDBC支持生成的键。需要适合的驱动。如果设置为true则这个设置强制生成的键被使用，尽管一些驱动拒绝兼容但仍然有效（比如Derby）默认：false -->
		<setting name="useGeneratedKeys" value="false" />
		<!-- <setting name="enhancementEnabled" value="false" /> -->
		<setting name="defaultExecutorType" value="SIMPLE" />
		<setting name="defaultStatementTimeout" value="25000" />
		 <setting name="logPrefix" value="dao."/>
	</settings>

	<!--<plugins>-->
		<!--<plugin-->
			<!--interceptor="com.dagather.wxmessage.mybatis.mysql.interceptor.DiclectStatementHandlerInterceptor" />-->
		<!--<plugin-->
			<!--interceptor="com.dagather.wxmessage.mybatis.mysql.interceptor.DiclectResultSetHandlerInterceptor" />-->
	<!--</plugins>-->

	<mappers>
		<!-- 基础数据 <mapper resource="mybatis/business/CmmAreaMapper.xml" /> -->
	</mappers>
</configuration>
